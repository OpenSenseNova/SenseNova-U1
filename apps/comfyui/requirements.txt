httpx
numpy
pillow
python-dotenv
# Local inference path. Use a GitHub-generated tarball, not git+https, so pip
# doesn't run `git submodule update --init --recursive` — that pulls
# evaluation/easi/* (hundreds of MB of benchmarking subrepos) which ComfyUI
# users never need. GitHub's archive tarball already strips submodule trees.
# Monorepo developers can install the four packages above directly, followed
# by `pip install -e .` from the repo root. Registry releases use the matching
# monorepo tag so installs do not drift with main.
sensenova-u1 @ https://github.com/OpenSenseNova/SenseNova-U1/archive/refs/tags/comfyui-v0.3.0.tar.gz
